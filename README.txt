Prototyping exercise

Attached is the code for a simple mobile app to load episodes from the Skylark API.

The app loads a JSON feed containing a list of episodes with title, image and description from a URL, then displays the contents of the feed in a scrollable list.


